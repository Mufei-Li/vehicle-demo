version: "3.8"
services:
  backend:
    build: ./backend
    volumes:
      - ./training/deploy:/app/models:ro
    ports:
      - "8000:8000"
    environment:
      - MODEL_WEIGHTS=/app/models/best.pt
    restart: unless-stopped
  # frontend 可以单独用 netlify/gh-pages 部署，或添加nginx容器来服务静态build
